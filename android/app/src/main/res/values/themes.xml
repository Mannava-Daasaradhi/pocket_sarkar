<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base theme: used by Android before Compose takes over.
        windowBackground matches our NeutralLight (#FFF8F0) so there's
        no white flash on launch.
    -->
    <style name="Theme.PocketSarkar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFF8F0</item>
        <item name="android:statusBarColor">#FFF8F0</item>
        <item name="android:navigationBarColor">#FFF8F0</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Splash screen theme (used before MainActivity starts) -->
    <style name="Theme.PocketSarkar.Splash" parent="Theme.PocketSarkar">
        <item name="android:windowBackground">#FFF8F0</item>
    </style>
</resources>
